//! Diagnostic Provider
//!
//! Clean diagnostic generation.

/// Diagnostic provider (placeholder)
#[derive(Debug)]
pub struct DiagnosticProvider;

impl DiagnosticProvider {
    pub fn new() -> Self {
        Self
    }
}

impl Default for DiagnosticProvider {
    fn default() -> Self {
        Self::new()
    }
}
